version: 3
projects:
- name: runatlantis
  dir: .
  branch: /main/
  workflow: makeworkflow
  autoplan:
    when_modified:
      - './modules/**/*.tf'
      - './modules/**/*.sh'
      - './modules/**/*.json'
      - '.terraform.lock.hcl'
      - '*.tfvars'
    enabled: true
  apply_requirements: [mergeable, approved]
